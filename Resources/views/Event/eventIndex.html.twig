{% extends 'OutlandishAcadOowpBundle:Default:defaultIndex.html.twig' %}

{% block searchResults %}

    {% for month in items %}

        {% if not loop.first %} <hr> {% endif %}

        <h3 class="block-heading">{{ month.month | raw }}</h3>

        {% for item in month['posts'] %}
            <li>
                {% block itemInclude %}
                    {% include 'OutlandishAcadOowpBundle:Default:defaultItem.html.twig' with {
                    'item': item,
                    'excerpt_length': 300,
                    'image': 'medium',
                    'classes': 'image-overlay'} %}
                {% endblock %}
            </li>
        {% endfor %}
    {% endfor %}

{% endblock %}

{% block noSearchResults %}

    <div class="row">
        <div class="columns-18 large">
            <p class="no-search-results">No future events found.</p>
        </div>
    </div>

{% endblock %}